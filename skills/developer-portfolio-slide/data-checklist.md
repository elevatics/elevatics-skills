# Data required from the user

Ask the user to provide as much of the following as possible before generating the slide:

* **Product name + logo**
  Example: `Spotify`

* **One-line product description**
  Example: `Music streaming and discovery platform`

* **Problem / value proposition**
  Example: `Discover and listen to music personalized to your taste`

* **Target users / use cases**
  Example: `Music listeners, playlist discovery, podcasts`

* **3–5 key features**
  Example: `Search, personalized recommendations, playlists, offline listening`

* **Architecture / workflow**
  Mermaid diagram, architecture diagram, README, or simple bullets.

* **Inputs / external data sources**
  Example: `Music catalog, artist metadata, listening activity`

* **Core processing steps**
  Example: `Ingest → analyze → personalize → serve`

* **Frontend / user experience**
  Example: `Home feed, search, playlist view, player`

* **Tech stack**
  Grouped into frontend, backend, AI/data, database, infrastructure, etc.

* **Important metrics**, if available
  Example: `10M tracks indexed · 5K requests/sec · <100 ms search`

* **1–2 product screenshots**

* **Optional:** live URL, GitHub URL, brand colors, reference slide/design.

Never invent data the user didn't supply — especially metrics. If a field is missing, omit that element from the slide rather than fabricating a value.
